# ====================
# Wallet Configuration
# ====================

# Bitcoin Network Selection
# Options: regtest, signet, testnet, bitcoin
# Default: signet
BITCOIN_NETWORK=regtest

# Esplora API Endpoint
# - Regtest: http://localhost:3000 (requires esplora-mock)
# - Signet: https://mempool.space/signet/api
# - Testnet: https://mempool.space/testnet/api
# - Mainnet: https://mempool.space/api
# Default: Determined by BITCOIN_NETWORK
ESPLORA_URL=http://localhost:3000

# Optional: Bitcoin Core RPC URL (for direct RPC access if needed)
# Format: http://rpcuser:rpcpass@host:port
# Default: Not used
# BITCOIN_RPC_URL=http://user:pass@127.0.0.1:18443

# ====================
# Integration Test Configuration (for tests only)
# ====================

# Funded BIP39 mnemonic for integration tests
# WARNING: For testing only! Never use mnemonics with real funds!
# Default: "abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"
TEST_MNEMONIC="abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"

# Fee rate for test transactions (sat/vB)
# - Regtest: 10-20 (arbitrary, but realistic)
# - Signet: 1-10 (low congestion)
# - Testnet: 10-50 (varies)
# Default: 20
TEST_FEE_RATE=20

# Test confirmation timeout (seconds)
# - Regtest: 60 (fast block generation)
# - Signet: 300-900 (blocks every ~1 minute)
# Default: 1800 (30 minutes)
TEST_TIMEOUT=1800

# ====================
# Logging Configuration
# ====================

# Rust log level
# Options: error, warn, info, debug, trace
# Can be scoped: RUST_LOG=wallet=debug,esplora_mock=info
# Default: RUST_LOG=wallet=debug,hyper=warn,reqwest=warn,rustls=warn
RUST_LOG=wallet=debug,hyper=warn,reqwest=warn,rustls=warn

# ============================================================================
# Server Configuration
# ============================================================================

# Bind address for the server
# - Use 127.0.0.1:3000 for local development (localhost only)
# - Use 0.0.0.0:3000 for production (accepts external connections)
# Default: 0.0.0.0:3000
BIND_ADDRESS=127.0.0.1:3000

# ============================================================================
# CORS Configuration
# ============================================================================

# Allowed origins for CORS (comma-separated)
# - Leave unset or empty for development (allows all origins with warning)
# - Set to your frontend URL(s) for production
# Examples:
#   Single origin: https://your-app.vercel.app
#   Multiple origins: https://your-app.vercel.app,https://your-app-preview.vercel.app
#   Include preview deployments: https://your-app.vercel.app,https://your-app-git-*.vercel.app
# Default: (empty - allows all origins)
# ALLOWED_ORIGINS=https://your-app.vercel.app

# ============================================================================
# Public URL Configuration
# ============================================================================

# Public URL of this wallet API server (for generating download links)
# Used for RGB consignment file downloads and genesis exports
# - Local development: http://localhost:3000
# - Production: Your public server URL with port
# Example: http://13.221.42.162:3000
# Default: http://localhost:3000
PUBLIC_URL=http://localhost:3000
